import type { RefObject } from "react";

export const contactHeadingRef: RefObject<HTMLElement | null> = { current: null };
export const bulldogSectionRef: RefObject<HTMLElement | null> = { current: null };
